#include<bits/stdc++.h>
using namespace std;

const int N = 1010,M = 15;
int n, m;
char str[N][M];
int f[][];

int main()
{
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n; i++)scanf("%s", str[i]);
	while (m--)
	{
		char query[M];
		int limit;

	}

	return 0;
}