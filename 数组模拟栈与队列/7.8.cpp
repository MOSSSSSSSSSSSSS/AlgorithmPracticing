

//栈先进后出，队列先进先出
#include<bits/stdc++.h>
using namespace std;

const int N = 100010;
int stk[N], tt;//tt是栈顶指针,开始指向0,栈中元素从下标1开始,0不放.tt指向最上面的元素

//插入
stk[++tt] = x;
//弹出
tt--;
//判断是否为空
if (tt > 0) not empty;
else empty;
//取栈顶元素
stk[tt];

//********************************队列               --------------------------------
                                            //             hh    hh++      tt      tt++
//队尾插入，队头弹出
int q[N], hh, tt = -1;
//插入  tt是-1，++后在0，  x 在下标为0的地方
q[++tt] = x;
//弹出
hh++;
//判断队列是否为空    一开始 hh>tt 是空，
if (hh <= tt) not empty;
else empty;
//取队头上的元素和队尾上的元素
q[hh];
q[tt];

//7.23栈
int stk[N], tt;
stk[++tt] = x;
tt--;
t > 0, not empty
stk[tt]
//7.23队列
q[N], hh = 0, tt = -1;
q[++tt] = x;
hh++
hh <= tt not empty
q[hh], q[tt];


